<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserRoleRefMapper">

    <!--  通用查询映射结果  -->
    <resultMap id="BaseResultMap" type="com.example.demo.model.UserRoleRef">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="role_id" property="roleId" />
        <result column="update_time" property="updateTime" />
        <result column="create_time" property="createTime" />
        <result column="update_by" property="updateBy" />
        <result column="create_by" property="createBy" />
        <result column="update_id" property="updateId" />
        <result column="create_id" property="createId" />
    </resultMap>

    <select id="selectMenuList" resultType="string" parameterType="long">
        select
        ah.menu_url
        from
        b_user ur,user_role_ref urr,role rl,role_auth_ref rar,auth ah
        where
        ur.id=#{userId}
        and
        ur.id=urr.user_id
        and
        urr.role_id=rl.id
        and
        rl.id=rar.role_id
        and
        rar.auth_id=ah.id
    </select>

</mapper>
